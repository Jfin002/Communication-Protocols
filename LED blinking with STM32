#include "STM32F4xx.h"
void delay();
int main()
{
	RCC-> AHB1ENR |=1<<3;
	GPIOD-> MODER &= ~(1<<31);
	GPIOD-> MODER |= 1<<30;
	while(1)
	{
		GPIOD-> ODR |= 1<<15;
		delay();
		GPIOD-> ODR &= ~(1<<15);
		delay();
	}
	return 0;
}
void delay()
{
	int i,j;
	for(i=0;i<1000;i++)
		for(j=0;j<1000;j++);
}
